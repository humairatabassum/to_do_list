import 'package:flutter/material.dart';

class TaskClass extends StatelessWidget {
  const TaskClass({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}